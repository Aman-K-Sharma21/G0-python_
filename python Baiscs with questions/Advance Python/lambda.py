#Lambda functions or anonymous functions

# def add(a,b):
#     return a + b

# add = lambda x,y: x + y
# minus = lambda x,y : x -y
# both above lines do the same thing

#--------------------------------------------
# def a_first(a):
#     return a[1]
# a = [[1,14],[5,26],[8,23]]
# # a.sort(key=a_first)
# a.sort(key=lambda x:x[1])
# print(a)
